{
  "name": "outtray",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first agentic document assistant: point it at the pile, get an action list.",
  "license": "MIT",
  "author": "Himmat Singh Khalsa",
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "build": "tsc -b",
    "eval:record": "pnpm build && node packages/evals/scripts/eval.mjs record",
    "eval:live": "pnpm build && node packages/evals/scripts/eval.mjs live",
    "eval:replay": "pnpm build && node packages/evals/scripts/eval.mjs replay"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.0",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
