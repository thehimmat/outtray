# @outtray/app

Placeholder. The Tauri v2 + React shell lands at the start of Phase 3, when the
action-queue approval UI exists to justify it (ADR-0001). Until then the CLI in
`packages/cli` is the demo surface, and the daily dev loop stays pure
Node/Vitest.

This directory intentionally has no `package.json` yet, so pnpm ignores it as a
workspace member.
